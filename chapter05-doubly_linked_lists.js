function DoublyLinkedList() {
  var Node = function (element) {
    this.element;
    this.next;
    this.prev; //NEW
  };

  var length = 0;
  var head = null;
  var tail = null; //NEW

  //methods here
}
